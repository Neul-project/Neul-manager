import axiosInstance from "@/lib/axios";
import { TargetDetailStyled } from "./styled";

//담당 피보호자 정보 모달
const TargetDetail = (props: { id: number }) => {
  const { id } = props;

  //**백엔드에서 요청하기

  return <TargetDetailStyled>dd</TargetDetailStyled>;
};

export default TargetDetail;
